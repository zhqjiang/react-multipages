{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch build",
            "program": "${workspaceFolder}/scripts/build.js"
        }
    ]
}